version: 0.2

phases:
  install:
    commands:
      - echo Installing

  build:
    commands:
      - echo Build started
      - echo Static files ready

  post_build:
    commands:
      - echo Build completed

artifacts:
  files:
    - '**/*'
